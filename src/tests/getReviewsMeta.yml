config:
  target: "http://localhost:3000"
  phases:
    - duration: 5
      arrivalRate: 1
      rampTo: 500
    - duration: 5
      arrivalRate: 500
scenarios:
  - flow:
      - get:
          url: "/reviews/meta"
          qs:
           product_id: 958320